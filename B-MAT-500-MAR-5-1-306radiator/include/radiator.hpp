/*
** EPITECH PROJECT, 2020
** ouioui
** File description:
** oui
*/

#pragma once

#include <string>
#include <vector>

static const bool SUCCESS = true;
static const bool FAILURE = false;

extern bool yonigg(int ac, char **av, int n, int ir, int jr);
extern bool allah(int ac, char **av, int n, int ir, int jr, int i, int j);
extern void hahohore(const std::vector<double> &matrixX);
extern void scoubite(const std::vector<double> &matrixX, int n, int j, int i);
extern void maisputinsimon(const std::vector<std::vector<double>> &matrixA);
extern void comehereyoulilshit(int n, int nPow, int jr, int ir, int i, int j, const std::string &options);